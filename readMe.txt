instruction for installing the project: 

1. install vscode and node.js
2. open the project folder on vscode
3. open the terminal of the vscode and type npm i -f
4. the type cd backend and type npm i -f
5. for runing the project clear the terminal and type npm run start

enjoy!